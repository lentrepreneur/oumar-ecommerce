{% extends 'base.html.twig' %}

{% block title %}réinitialisez votre mot de passe{% endblock %}

{% block body %}
    <div class="w-full my-8 flex items-center justify-center">
      <div class="w-[25%] max-md:w-[90%]">
        <h1>Réinitialisez votre mot de passe</h1>
        <div method="post" class="w-full flex flex-col items-center justify-center gap-8">
            {{ form_start(resetForm) }}
                {{ form_row(resetForm.plainPassword) }}
                <button class="bg-black text-white p-3">Réinitialisez votre mot de passe</button>
            {{ form_end(resetForm) }}
        </div>
      </div>
    </div>
{% endblock %}
